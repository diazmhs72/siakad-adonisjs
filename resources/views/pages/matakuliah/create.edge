@layout('layouts/app', { title: 'Tambah Matakuliah' })
  @section('body')
    <h2 class="text-2xl font-bold text-slate-800 mb-6">
      Tambah Matakuliah Baru
    </h2>
    <div class="bg-white shadow-lg rounded-lg p-6 max-w-xl">
      <form action="{{ route('matakuliah.store') }}" method="POST">
        {{ csrfField() }}
        @include('pages/matakuliah/form', { prodis: prodis, isEditMode: false })
        <div class="mt-6">
          <button type="submit" class="px-4 py-2 bg-indigo-600 text-white rounded-md">Simpan</button>
        </div>
      </form>
    </div>
  @endsection
